from fastapi import APIRouter

router = APIRouter()

"""
Future responsibility:
API routes to trigger, parameterize, and monitor signal analysis pipelines (DSP, ML, recovery).
Implementation will be added during the signal triage and ML phases.
"""
