# ParameterPanel Component

## Future Responsibility
Displays estimated parameters (SNR, bandwidth, carrier offset, symbol rate) and allows users to manually override or lock estimated parameters before demodulation.
Implementation will be added during the signal triage phase (Phase 2).
