# SignalOverview Component

## Future Responsibility
Displays high-level signal metadata (file format, sample rate, center frequency, duration) and summary stats immediately after file ingestion.
Implementation will be added during the file ingestion phase (Phase 1).
