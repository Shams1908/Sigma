# Serialized Models Directory

This directory is designated for storing serialized machine learning and deep learning models (e.g., PyTorch weights `.pt` or Scikit-learn models `.pkl`).

All production-ready models used by the `ml/inference/` engine are loaded from this directory.
