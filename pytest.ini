[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Add backend to path for all test runs
pythonpath = backend .
markers =
    timeout: mark a test with a maximum execution time (requires pytest-timeout or is informational)
